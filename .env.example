# Operator secrets for wrangler dev - copy to apps/zerosend/.dev.vars
# The Deploy to Cloudflare button reads repo root .dev.vars.example for the same variables.
ADMIN_TOKEN=change-me-to-a-long-random-string
SESSION_SECRET=change-me-run-openssl-rand-hex-32
